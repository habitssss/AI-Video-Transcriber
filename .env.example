# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Custom OpenAI Base URL (for proxy or alternative endpoints)
OPENAI_BASE_URL=https://oneapi.basevec.com/v1

# Model Configuration (Optional)
# Customize model names for different tasks
# Default values align with backend summarizer/translator
OPENAI_MODEL_FAST=deepseek-chat
OPENAI_MODEL_SMART=deepseek-chat

# Server Configuration (Optional)
HOST=0.0.0.0
PORT=8000
PRODUCTION_MODE=false

# Whisper Model Configuration (Optional)
# Options: tiny, base, small, medium, large
WHISPER_MODEL_SIZE=base
